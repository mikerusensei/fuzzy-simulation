{
  "name": "fuzzy-simulation",
  "version": "1.0.0",
  "description": "A simulation of fuzzy logic",
  "scripts": {
    "build": "parcel build --public-url https://mikerusensei.github.io/fuzzy-simulation src/index.html && rm -rf docs && mv dist docs && cp -r src/assets docs/assets",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
