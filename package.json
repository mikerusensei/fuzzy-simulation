{
  "name": "fuzzy-simulation",
  "version": "1.0.0",
  "description": "A simulation of fuzzy logic",
  "scripts": {
    "build": "parcel build src/index.html && cp -r src/assets dist/assets",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
